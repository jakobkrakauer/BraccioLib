#ifndef CONFIG_H
#define CONFIG_H

const unsigned int deltaT_ms = 50;   // Every 50 ms → calculate new angles → send to servos
const float max_speed = 45.0f;         // Maximum joint speed [deg/s]

#endif